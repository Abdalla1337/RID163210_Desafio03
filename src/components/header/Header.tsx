import { HeaderWrapper, ButtonHeader } from "./styles";

const Header = () => {
  return (
    <HeaderWrapper>
      <ButtonHeader>Projetos</ButtonHeader>
      <ButtonHeader>Blogs</ButtonHeader>
      <ButtonHeader>Contato</ButtonHeader>
    </HeaderWrapper>
  );
};

export default Header;
